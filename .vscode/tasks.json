{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildar",
            "type": "shell",
            "command": "mingw32-make clean && mingw32-make && ./main.exe && exit",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}